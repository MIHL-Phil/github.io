# Metadata inherited by all .qmd files under courses/
# Override per-course in courses/<name>/_metadata.yml

format:
  html:
    toc: true
    toc-depth: 3
    number-sections: false

# Each course page declares its own sidebar via the page-sidebar field:
# e.g.  sidebar: logic
